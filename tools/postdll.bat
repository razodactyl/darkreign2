@echo off
rem ---------------------------------------------------------------------------
rem Post-build step for DLL projects. Nothing references it, and it has been an
rem empty stub for as long as it has been in the repo. Kept as a no-op.
rem
rem See postbuild.bat for the history of the equivalent .exe step.
rem ---------------------------------------------------------------------------
exit /b 0
